# Endpoints to exclude from OpenAPI code generation
# One endpoint path per line (supports regex patterns)
# Lines starting with # are comments

/prompts/improve
/prompts/nudge

# Global scrape endpoints (use session-based scrape instead)
^/scrape$
^/scrape_from_html$

# Vault credit card operations
/vaults/\{vault_id\}/card
